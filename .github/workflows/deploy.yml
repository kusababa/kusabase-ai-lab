name: Deploy to AWS

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Build
        run: npm run build

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ap-northeast-3

      - name: Upload assets (long-term cache)
        run: |
          aws s3 sync dist/ s3://${{ secrets.S3_BUCKET }} \
            --delete \
            --exclude "*.html" \
            --exclude "robots.txt" \
            --exclude "sitemap*.xml" \
            --exclude "rss.xml" \
            --exclude "pagefind/*" \
            --exclude "logo.svg" \
            --exclude "og-default.svg" \
            --exclude "AILab_icon.png" \
            --exclude "AILab_logo.png" \
            --exclude "images/*" \
            --cache-control "public, max-age=31536000, immutable"

      - name: Upload HTML & meta files (no-cache)
        run: |
          aws s3 sync dist/ s3://${{ secrets.S3_BUCKET }} \
            --exclude "*" \
            --include "*.html" \
            --include "robots.txt" \
            --include "sitemap*.xml" \
            --include "rss.xml" \
            --include "pagefind/*" \
            --include "logo.svg" \
            --include "og-default.svg" \
            --include "AILab_icon.png" \
            --include "AILab_logo.png" \
            --include "images/*" \
            --cache-control "public, max-age=0, must-revalidate"

      - name: Invalidate CloudFront cache
        run: |
          aws cloudfront create-invalidation \
            --distribution-id ${{ secrets.CLOUDFRONT_DISTRIBUTION_ID }} \
            --paths "/*"

      - name: Notify LINE for newly published articles
        continue-on-error: true
        env:
          LINE_CHANNEL_ACCESS_TOKEN: ${{ secrets.LINE_CHANNEL_ACCESS_TOKEN }}
          GIT_DIFF_BEFORE: ${{ github.event.before }}
          GIT_DIFF_AFTER: ${{ github.sha }}
        run: npm run notify:line
